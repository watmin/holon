numpy>=1.21.0
edn_format>=0.7.0
pytest>=6.0.0
psutil>=5.8.0
fastapi>=0.100.0
uvicorn>=0.20.0
requests>=2.25.0
faiss-cpu>=1.7.0
httpx>=0.20.0
numba>=0.56.0
qdrant-client>=1.7.0

# Optional: TorchHD backend (recommended for best accuracy)
# torch-hd>=5.0.0  # Provides Level/Circular embeddings

# Optional: GPU acceleration with CuPy (for original encoder)
# cupy-cuda12x>=13.0.0  # For CUDA 12.x
# cupy-cuda11x>=13.0.0  # For CUDA 11.x

# Optional: PDF processing
# PyPDF2>=2.0.0  # For PDF processing in challenge demos
